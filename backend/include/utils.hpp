#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <chrono>

namespace ChatServer {
    std::string currentTimestamp();
    std::string trim(const std::string& str);
}

#endif // UTILS_HPP
